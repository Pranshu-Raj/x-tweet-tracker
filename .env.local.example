# X Growth Cockpit runs fully offline — no keys required for V1.
#
# OPTIONAL (Phase 2): to upgrade the Hook Lab from template-based rewrites to
# real AI generation, copy this file to .env.local and set a key. The app works
# without it.
#
# ANTHROPIC_API_KEY=sk-ant-...
